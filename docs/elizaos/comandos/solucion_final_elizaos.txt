PROBLEMA: ElizaOS está en bucle infinito mostrando la ayuda en lugar de iniciar el servidor.

SOLUCIÓN:

1. CONECTAR AL VPS:
   ssh -i vps_njalla_nueva markmur88@80.78.30.242

2. DETENER PROCESOS PROBLEMÁTICOS:
   pm2 stop all
   pm2 delete all

3. VERIFICAR CONFIGURACIÓN:
   cd ~/eliza-develop
   cat .env
   cat .eliza/agents/amara-complete/config.json

4. INICIAR AGENTE CORRECTAMENTE:
   source ~/.zshrc
   cd packages/cli
   pm2 start dist/index.js --name 'amara-complete' -- start

5. VERIFICAR ESTADO:
   pm2 list
   netstat -tlnp | grep -E ':(918[2-7]|919[0-1])'

6. VERIFICAR LOGS:
   pm2 logs amara-complete --lines 10

EL PROBLEMA PRINCIPAL ES QUE FALTA EL COMANDO "start" AL INICIAR EL AGENTE.

URLs DE ACCESO ESPERADAS:
- http://amara.coretransapi.com:9190
- http://80.78.30.242:9190 